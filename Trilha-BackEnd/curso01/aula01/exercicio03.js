// Declare duas variáveis, uma contendo um número e outra contendo uma string.
// Em seguida, combine-as em uma terceira variável usando template strings para 
// montar uma frase e exiba o resultado no console.
const numero = 472;
const logradouro = "Rua João Batista";
const endereco = "O endereço completo é:\n" + logradouro + " "+ numero ;

console.log(endereco);