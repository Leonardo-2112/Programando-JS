//Crie uma variável numérica com um valor. 
// Utilize um operador ternário para verificar 
// se esse valor é par ou ímpar. Exiba o resultado no console.

const valor = 12;

// % => resto da divisão
const decisao = valor % 2 === 0 ? 'par':'ìmpar';// Operador ternário
console.log(`O número digitado é ${decisao}`)
